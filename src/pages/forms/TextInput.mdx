import { theme, TextInput } from "nullwind";

import { Meta, Playground, Demo, ThemePreview } from "~/components";

<Meta
  title="TextInput"
  description="These are system-generated notifications with a high degree of criticality. They should be used when the system is unstable or there is a risk of loss of data or access to resources."
/>

# TextInput

These are system-generated notifications with a high degree of criticality. They should be used when the system is unstable or there is a risk of loss of data or access to resources.

## Usage

<Demo
  component={(props) => <TextInput class="w-80" {...props} />}
  configurator={[
    {
      name: "label",
      type: "text",
      value: "My Field",
      defaultValue: "",
    },
    {
      name: "corner",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "helper",
      type: "text",
      value: "This is a helper message",
      defaultValue: "",
    },
    {
      name: "error",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "required",
      type: "boolean",
      value: true,
      defaultValue: false,
    },
    {
      name: "disabled",
      type: "boolean",
      value: false,
      defaultValue: false,
    },
  ]}
  template={(props) => `
    import { TextInput } from 'nullwind';

    export default function Demo() {
      return <TextInput ${props} bind={this.field} />;
    }

`}/>

## Theme

<ThemePreview
  theme={{
    input: theme.input,
    label: theme.label,
    corner: theme.corner,
    textInput: theme.textInput,
    helper: theme.helper,
    error: theme.error,
  }}
/>
