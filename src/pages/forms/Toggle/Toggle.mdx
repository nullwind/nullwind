import { theme, Toggle } from "nullwind";
import { Meta, Playground, ThemePreview } from "~/components";
import Preview from "./Preview";

<Meta
  title="Toggle"
  description="Use the following toggle switch component to ask for a yes or no type of input from your users without the use of JavaScript."
/>

# Toggle

Use the following toggle switch component to ask for a yes or no type of input from your users without the use of JavaScript.

## Usage

```jsx
import { Toggle } from "nullwind";
```

## Examples

<Playground title="Default">
  <Playground.Preview>
    <Preview />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Toggle
      bind={this.active}
      label="I accept the terms and conditions"
      helper="You must accept the terms and conditions to continue"
    />
    ```
  </Playground.Code>
</Playground>

<Playground title="Disabled">
  <Playground.Preview>
    <Toggle disabled />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Toggle disabled />
    ```
  </Playground.Code>
</Playground>

## Theme

<ThemePreview
  theme={{
    inlineInput: theme.inlineInput,
    label: theme.label,
    corner: theme.corner,
    toggle: theme.toggle,
    helper: theme.helper,
    error: theme.error,
  }}
/>
