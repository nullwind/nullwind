import { theme, Radio } from "nullwind";

import { Meta, Demo, ThemePreview } from "~/components";

<Meta
  title="Radio"
  description="Group a series of buttons together on a single line or stack them in a vertical column."
/>

# Radio

Group a series of buttons together on a single line or stack them in a vertical column.

## Usage

<Demo
  component={(props) => <Radio {...props} />}
  configurator={[
    {
      name: "label",
      type: "text",
      value: "My Field",
      defaultValue: "",
    },
    {
      name: "helper",
      type: "text",
      value: "This is a helper message",
      defaultValue: "",
    },
    {
      name: "error",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "required",
      type: "boolean",
      value: true,
      defaultValue: false,
    },
    {
      name: "disabled",
      type: "boolean",
      value: false,
      defaultValue: false,
    },
  ]}
  template={(props) => `
    import { Radio } from 'nullwind';

    export default function Demo() {
      return <Radio ${props} bind={this.field} />;
    }

`}/>

## Theme

<ThemePreview
  theme={{
    inlineInputWrapper: theme.inlineInputWrapper,
    label: theme.label,
    corner: theme.corner,
    radio: theme.radio,
    helper: theme.helper,
    error: theme.error,
  }}
/>
