import { theme, Textarea } from "nullwind";

import { Meta, Playground, ThemePreview, Demo } from "~/components";

<Meta title="Textarea" description="Use the following code to create a textarea form element." />

# Textarea

Use the following code to create a textarea form element.

## Usage

<Demo
  component={(props) => <Textarea class="w-80" {...props} />}
  configurator={[
    {
      name: "label",
      type: "text",
      value: "My Field",
      defaultValue: "",
    },
    {
      name: "corner",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "helper",
      type: "text",
      value: "This is a helper message",
      defaultValue: "",
    },
    {
      name: "error",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "required",
      type: "boolean",
      value: true,
      defaultValue: false,
    },
    {
      name: "disabled",
      type: "boolean",
      value: false,
      defaultValue: false,
    },
  ]}
  template={(props) => `
    import { Textarea } from 'nullwind';

    export default function Demo() {
      return <Textarea ${props} bind={this.field} />;
    }

`}/>

## Theme

<ThemePreview
  theme={{
    input: theme.input,
    label: theme.label,
    corner: theme.corner,
    textarea: theme.textarea,
    helper: theme.helper,
    error: theme.error,
  }}
/>
