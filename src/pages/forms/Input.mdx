import { theme, Input } from "nullwind";

import { Meta, Playground, Demo, ThemePreview } from "~/components";

<Meta
  title="Input"
  description="Input is a component that allows users to enter text input in a form field."
/>

# Input

Input is a component that allows users to enter text input in a form field.

## Usage

<Demo
  component={(props) => <Input class="w-80" {...props} />}
  configurator={[
    {
      name: "label",
      type: "text",
      value: "My Field",
      defaultValue: "",
    },
    {
      name: "corner",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "helper",
      type: "text",
      value: "This is a helper message",
      defaultValue: "",
    },
    {
      name: "error",
      type: "text",
      value: "",
      defaultValue: "",
    },
    {
      name: "required",
      type: "boolean",
      value: true,
      defaultValue: false,
    },
    {
      name: "disabled",
      type: "boolean",
      value: false,
      defaultValue: false,
    },
  ]}
  template={(props) => `
    import { Input } from 'nullwind';

    export default function Demo() {
      return <Input ${props} bind={this.field} />;
    }

`}/>

## Theme

<ThemePreview
  theme={{
    input: theme.input,
    label: theme.label,
    corner: theme.corner,
    input: theme.input,
    helper: theme.helper,
    error: theme.error,
  }}
/>
