import { theme, Checkbox } from "nullwind";

import { Meta, Playground, ThemePreview } from "~/components";

<Meta
  title="Checkbox"
  description="The code below can be used to create a fieldset of checkbox elements inside a form. We also created a checkbox variant with extra description and one in a disabled form."
/>

# Checkbox

The code below can be used to create a fieldset of checkbox elements inside a form. We also created a checkbox variant with extra description and one in a disabled form.

## Usage

```jsx
import { Checkbox } from "nullwind";
```

## Examples

<Playground title="Default">
  <Playground.Preview>
    <Checkbox id={1} label="Some label here" />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Checkbox id={1} label="Some label here" />
    ```
  </Playground.Code>
</Playground>

<Playground title="With helper">
  <Playground.Preview>
    <Checkbox id={2} label="Some label here" helper="This is some description" />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Checkbox id={2} label="Some label here" helper="This is some description" />
    ```
  </Playground.Code>
</Playground>

<Playground title="With validation error">
  <Playground.Preview>
    <Checkbox id={2} label="Some label here" error="This a error message" />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Checkbox id={2} label="Some label here" error="This a error message" />
    ```
  </Playground.Code>
</Playground>

<Playground title="Disabled">
  <Playground.Preview>
    <Checkbox id={3} label="Some label here" disabled />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Checkbox id={3} label="Some label here" disabled />
    ```
  </Playground.Code>
</Playground>

## Theme

<ThemePreview
  theme={{
    inlineInput: theme.inlineInput,
    label: theme.label,
    corner: theme.corner,
    checkbox: theme.checkbox,
    helper: theme.helper,
    error: theme.error,
  }}
/>
