import { theme, Tabs } from "nullwind";
import Preview from "./Preview";
import { Meta, Demo, ThemePreview } from "~/components";

<Meta
  title="Tabs"
  description="These are system-generated notifications with a high degree of criticality. They should be used when the system is unstable or there is a risk of loss of data or access to resources."
/>

# Tabs

Use these responsive tabs components to create a secondary navigational hierarchy for your website or toggle content inside a container

## Usage

<Demo
  component={Preview}
  template={() => `
    import { Tabs } from 'nullwind';

    export default function Demo() {
      return (
        <Tabs onchange={(index) => (this.active = index)}>
          <Tabs.Tab title="Profile" active>
            Tabs content 1
          </Tabs.Tab>
          <Tabs.Tab title="Notifications">
            Tabs content 2
          </Tabs.Tab>
          <Tabs.Tab title="Settings">
            Tabs content 3
          </Tabs.Tab>
        </Tabs>
      );
    }

`}/>

## Theme

<ThemePreview
  theme={{
    tabs: theme.tabs,
  }}
/>
