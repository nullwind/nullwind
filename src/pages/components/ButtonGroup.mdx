import { theme, Button, ButtonGroup } from "nullwind";

import { Meta, Demo, ThemePreview } from "~/components";

<Meta title="ButtonGroup" description="ButtonGroup component renders a group of buttons" />

# ButtonGroup

ButtonGroup component renders a group of buttons.

You can change style properties in `theme.buttonGroup`.

## Usage

<Demo
  component={() => (
    <ButtonGroup>
      <Button>Button 1</Button>
      <Button>Button 2</Button>
      <Button>Button 3</Button>
    </ButtonGroup>
  )}
  template={() => `
    import { ButtonGroup, Button } from 'nullwind';

    export default function Demo() {
      return (
        <ButtonGroup>
          <Button>Button 1</Button>
          <Button>Button 2</Button>
          <Button>Button 3</Button>
        </ButtonGroup>
      );
    }

`}/>

## Theme

<ThemePreview
  theme={{
    buttonGroup: theme.buttonGroup,
  }}
/>
