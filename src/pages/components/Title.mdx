import { Title } from "nullwind";

import { Meta, Playground } from "~/components";

<Meta
  title="Title"
  description="Use Title component to render h1-h6 headings with Mantine theme styles."
/>

# Title

Use Title component to render h1-h6 headings with Mantine theme styles. By default, Title has no margins and paddings. You can change headings font-size, font-weight and line-height properties in `theme.title`.

Set `h` prop to render specific element (h1-h6), default `h` is 1:

## Usage

```jsx
import { Title } from "nullwind";

const MyComponent = () => {
  return <Title h={1}>Welcome!</Title>;
};
```

## Examples

<Playground>
  <Playground.Preview>
    <Title h={1}>This is h1 title!</Title>
    <Title h={2}>This is h2 title!</Title>
    <Title h={3}>This is h3 title!</Title>
    <Title h={4}>This is h4 title!</Title>
    <Title h={5}>This is h5 title!</Title>
    <Title h={6}>This is h6 title!</Title>
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Title h={1}>This is h1 title!</Title>
    <Title h={2}>This is h2 title!</Title>
    <Title h={3}>This is h3 title!</Title>
    <Title h={4}>This is h4 title!</Title>
    <Title h={5}>This is h5 title!</Title>
    <Title h={6}>This is h6 title!</Title>
    ```
  </Playground.Code>
</Playground>
