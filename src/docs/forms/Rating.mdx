import { Meta, Playground } from "~/components/docs";
import { Rating } from "~/components";

<Meta title="Rating" description="Rating component is used to display rating stars." />

# Rating

Rating component is used to display rating stars.

## Usage

```jsx
import { Rating } from "nullwind";
```

## Examples

<Playground title="Default">
  <Playground.Preview>
    <Rating size={20} averageRate={3} />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Rating size={20} averageRate={3} bind={this.rating} />
    ```
  </Playground.Code>
</Playground>

<Playground title="Read only">
  <Playground.Preview>
    <Rating size={20} averageRate={2} readOnly />
  </Playground.Preview>
  <Playground.Code>
    ```jsx
    <Rating size={20} averageRate={2} readOnly bind={this.rating} />
    ```
  </Playground.Code>
</Playground>
